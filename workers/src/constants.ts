/**
 * Application-wide constants
 *
 * Purpose: Extract magic numbers and strings into named constants
 * Issue: GitHub Issue #10 - Remove magic numbers from codebase
 */

// Multi-tenancy support (MVP: single tenant)
export const TENANT_ID = 'default';

// Pagination defaults
export const DEFAULT_PAGE_SIZE = 50;
export const MAX_PAGE_SIZE = 500;
